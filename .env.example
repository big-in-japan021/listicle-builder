# Copie este arquivo para .env.local e preencha com suas chaves reais.
# NUNCA commite o .env.local (já está no .gitignore).

# OpenAI — usado para gerar imagens (gpt-image-1)
OPENAI_API_KEY=sk-...

# Anthropic — usado para estruturar a copy (claude-sonnet-4-5+)
ANTHROPIC_API_KEY=sk-ant-...
